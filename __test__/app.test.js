describe('PORT', () => {
    test('Active PORT', () => {

    })
})

describe('Positive tests: HTTP method requests', () => {
    test('GET method to display all items in data.json', () => {
        
    })

    test('POST method. Add a new item to data.json', () => {
        
    })

    test('PUT method. Update the value of "purchased" from false to true in data.json', () => {
        
    })

    test('DELETE method. Remove the item from data.json', () => {
        
    })
})

describe('Negative tests: HTTP method requests', () => {
    test('POST method. Missing "name"', () => {
        
    })

    test('POST method. Missing "quantity"', () => {
        
    })

    test('POST method. Missing "price"', () => {
        
    })

    test('PUT method. Missing "name"', () => {
        
    })

    test('DELETE method. Missing "name"', () => {
        
    })

    test('Other methods. PATCH is not supported', () => {
        
    })
})
